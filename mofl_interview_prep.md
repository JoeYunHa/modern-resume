# mofl Software Engineer 면접 준비 문서

> 작성일: 2026-05-08 | 대상 포지션: Software Engineer @ mofl Inc.

---

## 목차

1. [회사 및 직무 분석 요약](#1-회사-및-직무-분석-요약)
2. [나의 경험과 직무 연결 전략](#2-나의-경험과-직무-연결-전략)
3. [30초 자기소개](#3-30초-자기소개)
4. [예상 질문 & 맞춤 답변](#4-예상-질문--맞춤-답변)
5. [기술 면접 대비](#5-기술-면접-대비)
6. [약점 방어 전략](#6-약점-방어-전략)
7. [역질문 리스트](#7-역질문-리스트)
8. [수요예측 도메인 기초 지식](#8-수요예측-도메인-기초-지식)
9. [최종 체크리스트](#9-최종-체크리스트)

---

## 1. 회사 및 직무 분석 요약

### mofl은 무엇을 하는 회사인가

**한 줄 정의**: AI 시계열 예측으로 실물경제의 수요·공급 불균형 문제를 해결하는 B2B SaaS 회사

| 항목 | 내용 |
|------|------|
| 핵심 기술 | Time-series Forecasting AI |
| 주요 고객 | 패션 리테일, 소비재, 원자재, 택시 수요 등 |
| 제품 형태 | B2B SaaS / 대시보드 / 업무 자동화 / 수요예측 기반 의사결정 |
| 대표 성과 | M5 월마트 글로벌 수요예측 대회 3위(5,558팀), 2020 AI 챔피언십 우승 |
| 정량 성과 | 품절 55% 감소, 업무시간 6h → 0.5h, 매일 2,000만여 개 수요 예측 |

### 직무가 실제로 요구하는 것

이 포지션은 **AI 모델 연구자가 아니라**, 예측 결과를 고객의 실제 비즈니스 로직·자동화·대시보드로 제품화하는 엔지니어다.

| 우선순위 | 핵심 역량 | 이유 |
|----------|----------|------|
| 1 | 복잡한 비즈니스 로직을 코드로 안정적으로 구현 | 공고에 "복잡한 비즈니스 로직", "고객 customization", "low-code SaaS화" 반복 강조 |
| 2 | 데이터 파이프라인 · DB · API · 운영 지표 설계 | 주요업무에 데이터 파이프라인, 핵심 운영 지표, API 설계 명시 |
| 3 | 운영 중 서비스 리팩토링·안정성 개선 | 기존 서비스 구조 개선, 고가용성, fail-over 요구 |
| 4 | 고객 문제를 이해하고 기능을 설계 | "고객에게 큰 가치를 제공", "고객 관점 문제 이해"가 자격요건에 포함 |

### 기술 스택 (공고 기준)

| 영역 | 스택 |
|------|------|
| 언어 | Python, Node.js |
| DB | PostgreSQL, MySQL, MongoDB |
| 캐시 | Redis |
| 파이프라인 | Airflow, Spark |
| 인프라 | 자체 온프레미스 서버 + 클라우드 병행 |

---

## 2. 나의 경험과 직무 연결 전략

### 프로젝트별 연결 포인트

| 내 경험 | mofl 직무 연결점 |
|--------|----------------|
| **kindMap** Python FastAPI + PostgreSQL + AWS | 데이터 파이프라인, DB 설계, API 서버, 클라우드 운영 |
| **kindMap** XGBoost 혼잡도 예측 모델 (R²=0.9151) | AI 예측 결과를 서비스에 연결하는 경험 |
| **kindMap** 알고리즘 19분 → 1초, RPS 75배 향상 | 운영 환경에서 성능 병목 진단 및 구조 개선 |
| **수담** Redis 캐싱 (정적 데이터 O(1) 조회) | 고가용성·성능·운영 안정성 설계 |
| **수담** AI팀과 I/O Spec 협의 → 선제적 테스트 환경 구축 | AI 팀과 백엔드 협업 경계 설정 경험 |
| **수담** 응답시간 1000ms → 100ms (90% 이상 감소) | 운영 서비스 병목 분석 및 아키텍처 재설계 |
| **RCPVMS** PyTorch 앙상블 모델 + XAI (Integrated Gradients) | 예측 결과의 explainability 고려 경험 |
| **NIMS** 커스텀 바이너리 파서, 복잡한 도메인 로직 구현 | 도메인 특화 비즈니스 로직을 구조화하는 역량 |

### 포지셔닝 전략

**잘못된 포지셔닝:**
> "저는 AI 모델을 만들어봤습니다."
> "저는 여러 프로젝트를 개발했습니다."

**올바른 포지셔닝:**
> "저는 **데이터를 수집·정제하고, 도메인 지표를 계산해 API와 화면으로 제공하는 end-to-end 데이터 제품** 개발에 강점이 있습니다. 특히 AI 팀의 예측 결과가 실제 서비스에서 안정적으로 동작하도록 파이프라인, 캐싱, 운영 구조를 설계하는 역할을 반복적으로 수행했습니다."

---

## 3. 30초 자기소개

> 저는 데이터를 수집하고 정제한 뒤, 도메인 지표를 계산해 API와 화면으로 제공하는 end-to-end 데이터 제품 개발에 관심이 많은 백엔드 엔지니어입니다.
>
> 교통약자 경로 탐색 서비스에서는 Python과 FastAPI로 경로 탐색 알고리즘의 수행 시간을 19분에서 1초 이내로 줄였고, 실시간 수어 번역 서비스에서는 AI 팀과 I/O 스펙을 협의하며 선제적 테스트 환경을 구축하고, Redis 캐싱과 아키텍처 재설계로 응답 시간을 90% 이상 개선했습니다.
>
> mofl의 포지션은 단순 CRUD 백엔드가 아니라, 수요예측 결과를 실제 고객의 재고·공급 의사결정으로 연결하는 역할로 이해했습니다. 복잡한 도메인 로직을 안정적으로 구조화하고, 고객이 신뢰할 수 있는 데이터 제품으로 만드는 부분에서 기여할 수 있다고 생각합니다.

---

## 4. 예상 질문 & 맞춤 답변

### Q1. mofl이 어떤 회사라고 이해했나요?

> mofl은 시계열 예측 AI를 기반으로 실물경제의 수요와 공급을 최적화하는 B2B SaaS 회사로 이해했습니다.
>
> 특히 패션 리테일처럼 매장, SKU, 시즌, 가격, 프로모션, 재고 상태가 복잡하게 얽힌 영역에서 수요를 예측하고, 그 결과를 재고 보충·물류 이동·업무 자동화로 연결하는 것이 핵심이라고 봤습니다.
>
> M5 월마트 대회 3위, 매일 2,000만 개 이상의 수요 예측, 품절 55% 감소 같은 수치는 단순히 모델 성능이 좋은 것을 넘어서 그 결과가 실제 고객 업무에 연결되어 있다는 것을 보여준다고 생각합니다.

---

### Q2. 이 포지션에서 가장 중요하다고 생각하는 역량은?

> 세 가지로 생각합니다.
>
> 첫째는 복잡한 도메인 로직을 코드로 명확하게 표현하는 능력입니다. 수요예측 결과를 재고 보충·이동 추천으로 바꾸는 로직은 도메인 룰이 매우 복잡하고 고객마다 다를 수 있습니다.
>
> 둘째는 데이터 정합성과 장애 상황을 고려한 파이프라인 설계입니다. 예측 모델이 좋아도 입력 데이터가 깨지거나 배치가 실패하면 고객은 사용할 수 없습니다.
>
> 셋째는 예측 결과를 고객이 신뢰할 수 있는 API와 화면으로 제공하는 제품화 역량입니다. 예측값 자체보다도, 그것이 고객 의사결정으로 이어지도록 만드는 구조가 핵심이라고 생각합니다.

---

### Q3. 본인의 프로젝트 중 이 직무와 연결되는 경험은?

> 두 프로젝트를 말씀드리고 싶습니다.
>
> **kindMap 프로젝트**에서는 서울교통공사 Open API와 자체 데이터를 통합해 교통약자 유형별 경로를 계산하고, 이를 PostgreSQL에 적재한 뒤 FastAPI로 제공했습니다. 또한 XGBoost로 지하철 혼잡도 예측 모델을 만들고 그 결과를 서비스에 연결했습니다. 여기서 중요했던 것은 단순히 모델이 좋은 게 아니라, 예측 결과가 경로 안내라는 실제 의사결정으로 연결되는 구조를 만드는 것이었습니다.
>
> **수담 프로젝트**에서는 AI 팀과 I/O 스펙을 사전 협의해 모델 이식 환경을 선제적으로 구축했고, Redis로 정적 데이터를 캐싱해 DB 불필요 접근을 제거하며, 응답 시간을 1000ms에서 100ms로 줄였습니다. 이 경험은 mofl에서 AI 예측 결과를 안정적으로 서비스화하는 역할과 직접 연결된다고 생각합니다.

---

### Q4. 복잡한 비즈니스 로직을 어떻게 관리하나요?

> 처음부터 코드로 들어가지 않고, 상태와 규칙을 먼저 분리합니다.
>
> kindMap에서 교통약자 유형별 경로 탐색 로직을 구현할 때, 입력 조건(사용자 유형, 출발지, 목적지), 중간 계산값(각 엣지의 가중치), 최종 결과(추천 경로), 예외 케이스(경로 없음, 데이터 누락)를 먼저 명확히 정의했습니다. 그다음 각 단계를 독립적으로 테스트 가능한 단위로 쪼개고, 공통 로직은 서비스 계층으로 분리했습니다.
>
> 고객별 커스터마이징이 필요한 경우에는 조건문이 무한히 늘어나지 않도록 rule/config 기반으로 관리하는 방향을 선호합니다. RCPVMS 프로젝트에서도 이상 탐지 기준을 하드코딩하지 않고, OOD 탐지 임계값과 앙상블 가중치를 설정으로 분리해 유지보수성을 높였습니다.

---

### Q5. 운영 중인 코드를 리팩토링할 때 어떻게 하나요?

> 운영 안정성을 항상 최우선으로 생각합니다.
>
> 수담 프로젝트에서 아키텍처를 재설계할 때, 기존 프록시 계층을 바로 제거하지 않고 먼저 응답 시간과 대역폭 지표를 수집해 실제 병목을 확인했습니다. 그 다음 기능 변경과 구조 변경을 분리해서, 프록시 제거 → WebSocket 직접 연결 → 성능 검증 순서로 단계적으로 적용했습니다.
>
> 일반적으로 저는 (1) 현재 동작을 보존하는 회귀 테스트를 먼저 만들고, (2) 로그와 지표로 실제 사용 경로를 확인한 뒤, (3) 기능 변경과 구조 변경을 분리해 작은 단위로 배포합니다. 데이터 파이프라인이나 API 응답 구조는 하위 호환성을 깨지 않도록 버전 관리나 adapter 계층을 고려합니다.

---

### Q6. AI 기반 서비스에서 백엔드가 주의할 점은?

> AI 서비스의 백엔드는 단순히 모델 결과를 전달하는 레이어가 아니라고 생각합니다.
>
> RCPVMS 프로젝트에서 AI 이상 탐지 결과를 서비스로 만들 때, 결과의 confidence, 버전 관리, OOD(분포 외 입력) 탐지 플래그, 수동 override 기능까지 함께 설계했습니다. 예측값이 항상 맞지 않기 때문에 결과를 무조건 신뢰하지 않고, 이상치 탐지와 fallback 로직을 함께 구현하는 것이 중요했습니다.
>
> 또한 수담에서 AI 팀과 협업할 때, 모델 입력·출력 스키마를 명확히 정의하고 선제적 테스트 환경을 구축했던 것처럼, 입력 데이터 품질 검증과 예측 결과 재현성 확보가 백엔드 책임이라고 생각합니다.

---

### Q7. 왜 mofl인가?

> mofl에 관심을 가진 이유는 AI 모델 자체보다도, 그 결과가 실제 고객의 비용 절감과 업무 자동화로 이어지는 구조가 명확하기 때문입니다.
>
> 수요예측은 예측값만 좋다고 끝나는 문제가 아닙니다. 데이터 정합성, 예측 결과 저장 구조, 재고 이동 추천 로직, 운영 지표, 대시보드, 고객별 커스터마이징이 모두 맞물려야 실제 가치가 생깁니다.
>
> 저는 kindMap과 수담에서 AI 예측 결과를 서비스로 연결하는 경험을 했고, 그 과정에서 데이터 흐름 전체를 설계하고 운영하는 역할에 강점이 있다는 것을 확인했습니다. mofl에서는 더 큰 규모와 실제 B2B 고객을 대상으로 이 역할을 수행하고 싶습니다.

---

## 5. 기술 면접 대비

### 5.1 데이터 모델링 설계 문제

**예상 질문:** "매장 700개, SKU 30,000개, 일 단위 예측 결과를 저장하고 조회해야 한다면 어떻게 설계하겠습니까?"

**답변 방향:**

```
핵심 테이블:
- store (store_id, name, region, ...)
- sku (sku_id, category, brand, ...)
- sales (store_id, sku_id, date, qty)
- forecast (store_id, sku_id, forecast_date, predicted_qty, model_version, generated_at)
- recommendation (store_id, sku_id, date, action, qty, reason)
- audit_log (id, entity_type, entity_id, changed_at, before, after)

인덱스 전략:
- forecast: (store_id, sku_id, forecast_date) 복합 인덱스
- forecast_date 기준 파티셔닝으로 오래된 데이터 관리
- 대시보드용 materialized view 또는 pre-aggregation

캐싱 전략:
- 자주 조회하는 기간/매장/카테고리 기준 Redis 캐싱
- TTL은 예측 생성 주기에 맞게 설정

재현성 보장:
- model_version, generated_at 저장
- 배치 실패 시 idempotency key로 중복 실행 방지
```

---

### 5.2 데이터 파이프라인 설계

**예상 질문:** "고객사 데이터가 매일 들어오고, 예측 결과를 생성한 뒤 대시보드에 반영해야 한다면?"

**답변 방향 (Airflow DAG 기준):**

```
1. 데이터 수집 (고객사 DB / SFTP / API)
2. schema validation (컬럼 타입, 필수값)
3. 정합성 체크 (날짜 범위, 매장-SKU 매핑 유효성)
4. 결측/이상치 처리 (stockout bias 보정, 이상값 capping)
5. feature 생성 (seasonality, promotion flag, lag feature)
6. 모델 추론 요청 (AI 팀 서비스 API 호출)
7. 예측 결과 저장 (forecast 테이블, model_version 기록)
8. 추천 로직 실행 (재고 보충/이동 추천 생성)
9. 대시보드 집계 갱신 (materialized view refresh)
10. 알림/로그/모니터링 (SLA miss, 이상 데이터 알림)

Airflow 고려사항:
- 각 태스크를 idempotent하게 설계
- retry, backfill 정책 명시
- SLA miss 알림 설정
- 고객사별 DAG 분리 또는 파라미터화
```

---

### 5.3 SQL 실력 문제

**예상 질문:** "매장별 주간 결품률과 SKU별 예측 오차(MAPE)를 계산하는 쿼리를 작성해보세요."

```sql
-- 매장별 주간 결품률
SELECT
    store_id,
    DATE_TRUNC('week', date) AS week,
    COUNT(*) FILTER (WHERE actual_stock = 0 AND actual_sales > 0) AS stockout_count,
    COUNT(*) AS total_count,
    ROUND(
        COUNT(*) FILTER (WHERE actual_stock = 0 AND actual_sales > 0)::numeric / COUNT(*) * 100, 2
    ) AS stockout_rate_pct
FROM daily_inventory
GROUP BY store_id, DATE_TRUNC('week', date);

-- SKU별 예측 오차 (MAPE)
SELECT
    sku_id,
    AVG(ABS(actual_qty - predicted_qty) / NULLIF(actual_qty, 0)) * 100 AS mape
FROM forecast f
JOIN sales s USING (store_id, sku_id, forecast_date)
WHERE forecast_date >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY sku_id
ORDER BY mape DESC;
```

---

### 5.4 시스템 설계

**예상 질문:** "대규모 예측 결과를 저장하고 실시간 대시보드에서 빠르게 조회하는 구조는?"

**답변 방향:**

```
Write Path:
- Airflow 배치 → 예측 결과 bulk insert → partition table
- 집계 결과는 별도 summary table에 pre-compute

Read Path:
- 대시보드 API → Redis 캐시 확인 → cache miss시 DB 조회
- 자주 쓰는 필터(매장, 카테고리, 기간)에 composite index
- 대용량 집계는 materialized view 활용

확장성 고려:
- 고객사별 schema separation 또는 tenant_id 컬럼
- 오래된 예측 데이터는 cold storage로 이관
- Spark로 대규모 feature 생성 (일 단위 배치)
```

---

### 5.5 Redis 활용

**kindMap/수담 경험 기반 답변 포인트:**

- 정적 데이터(매장 정보, SKU 마스터) → TTL 길게 설정, 캐시 워밍
- 예측 결과 캐시 → 배치 완료 후 invalidation
- idempotency key → 중복 배치 실행 방지
- 작업 상태 저장 → 배치 진행 상태 추적

---

## 6. 약점 방어 전략

### 약점 1: Node.js 경험 상대적 부족

공고는 Node.js와 Python 능숙을 요구한다.

**방어 답변:**
> 백엔드 주력은 Python/FastAPI와 Java/Spring이지만, 강조하고 싶은 부분은 특정 프레임워크보다 API 설계, DB 모델링, 데이터 흐름, 운영 안정성의 사고 방식입니다. NIMS 프로젝트에서 Node.js N-API를 사용한 경험이 있고, JavaScript/TypeScript 기반 프로젝트에서 async 처리와 이벤트 루프 구조를 이해하고 있습니다. Node.js는 빠르게 적응할 수 있습니다.

---

### 약점 2: 상용 B2B SaaS 운영 경험 부족

**방어 답변:**
> 실제 유료 B2B 운영 경험은 아직 제한적입니다. 다만 RealGain에서 기술 자문 계약직으로 실제 산업 현장(원자력 발전소)에 배포되는 소프트웨어를 개발했고, 그 과정에서 안정성, 장애 복구, 도메인 정확성에 대한 실질적 압박을 경험했습니다. 개인 프로젝트에서도 Redis 연결 장애, 배포 환경 차이, API 성능 문제 같은 운영형 문제를 직접 해결했습니다.

---

### 약점 3: Airflow/Spark 실전 경험 부족

**방어 답변:**
> Airflow와 Spark를 프로덕션에서 운영한 경험은 아직 없습니다. 다만 kindMap에서 AWS Lambda와 배치 파이프라인을 설계하면서 DAG 구조, 재시도 정책, idempotency 설계의 중요성을 이해했습니다. Airflow의 DAG 기반 스케줄링과 backfill 개념은 학습했고, 실무에서 빠르게 적용할 수 있도록 준비 중입니다.

---

### 약점 4: AI 모델 연구자가 아님

**방어 답변 (오히려 강점으로 전환):**
> 모델 연구보다는 모델이 안정적으로 서비스에 반영되고 고객 업무 로직과 연결되도록 만드는 역할에 더 강점이 있습니다. RCPVMS에서 PyTorch 앙상블 모델을 구현하면서 동시에 OOD 탐지, XAI(Integrated Gradients), Python Daemon Pool 같은 운영 안정성 요소를 함께 설계했습니다. 모델 입력 데이터 품질, 예측 결과 versioning, fallback 로직이 백엔드 책임이라고 생각합니다.

---

## 7. 역질문 리스트

Coffee Chat은 상호 적합도를 확인하는 자리다. **문제 이해도와 실무 적합도**를 보여주는 질문을 준비한다.

### 추천 질문 (우선순위 순)

**Q1. 업무 비중 파악**
> 현재 Software Engineer가 가장 많이 다루는 문제는 신규 기능 개발, 기존 코드 리팩토링, 데이터 파이프라인 안정화, 고객사별 커스터마이징 중 어디에 가장 가깝습니까?

**Q2. 고객별 룰 관리**
> 수요예측 결과가 재고 이동·보충 의사결정으로 이어질 때, 고객사별 룰 차이는 코드 레벨에서 어떻게 관리하고 계신가요? rule/config 기반인지, 고객별 모듈 분리 방식인지 궁금합니다.

**Q3. AI 팀과 백엔드 협업 경계**
> 백엔드와 AI 팀의 협업 경계는 어떻게 나뉘어 있나요? feature 생성, 모델 입력 스키마, 예측 결과 검증, 추천 로직 중 Software Engineer가 어디까지 관여하는지 궁금합니다.

**Q4. 데이터 품질 이슈**
> 데이터 파이프라인에서 가장 자주 발생하는 문제는 고객사 원천 데이터 품질, 스키마 변경, 결측/이상치, 배치 실패 중 어느 쪽인가요?

**Q5. Explainability**
> 예측 결과의 정확도 외에도, 고객이 결과를 신뢰하도록 만드는 explainability나 근거 표시 기능도 제품에서 중요하게 다루고 있는지 궁금합니다.

**Q6. SaaS화 단계**
> 공고에서 low-code SaaS화가 challenging한 문제라고 되어 있는데, 현재는 고객별 커스터마이징 비중이 높은 단계인지, 공통 플랫폼화로 전환하는 단계인지 궁금합니다.

---

## 8. 수요예측 도메인 기초 지식

Software Engineer라도 최소한 이 개념들은 알고 가야 한다.

| 개념 | 설명 | mofl 연결점 |
|------|------|-------------|
| **seasonality** | 요일, 월, 계절, 명절, 이벤트에 따른 수요 패턴 | feature 생성 시 고려 |
| **promotion effect** | 할인·프로모션·가격 변경이 수요에 미치는 영향 | 예측 입력 feature |
| **stockout bias** ⭐ | 재고 없음 → 판매 0 → 수요 0으로 오해하는 문제 | 핵심 도메인 이슈 |
| **cold start** | 신규 상품·매장의 과거 데이터 부족 | meta learning으로 대응 |
| **hierarchy** | 전체-카테고리-SKU-매장 단위 예측 일관성 | 집계 레벨 설계 |
| **backtesting** | 과거 시점 기준으로 미래 예측해 성능 검증 | 모델 평가 방법론 |
| **leakage** | 미래 정보를 학습/검증에 잘못 포함 | 파이프라인 설계 주의점 |
| **explainability** | 고객이 추천 이유를 이해할 수 있게 하는 장치 | 백엔드 근거 표시 기능 |

> **stockout bias는 특히 중요하다.** 판매량 데이터는 "수요"가 아니라 "재고가 있었을 때 관측된 판매량"이다. 품절 상태에서는 실제 수요가 있어도 판매량이 0에 가까워질 수 있다. 이 문제를 인지하고 있음을 언급하면 도메인 이해도가 높아 보인다.

---

## 9. 최종 체크리스트

### 면접 전 준비 우선순위

- [ ] **mofl 1분 설명 암기**
  - "AI 시계열 예측 기반 수요·공급 최적화 B2B SaaS"
  - "패션 리테일 재고·품절·물류 비효율 해결"
  - "M5 월마트 대회 3위, 매일 2,000만 개 수요 예측, 품절 55% 감소"

- [ ] **본인 프로젝트 2분 설명 정리**
  - kindMap: 19분 → 1초, RPS 75배, AI 예측 서비스화 경험
  - 수담: 1000ms → 100ms, AI팀 협업, Redis 캐싱

- [ ] **Node.js 약점 방어 준비**
  - NIMS N-API 경험, TypeScript 사용 경험 강조
  - 빠른 적응 가능성 언급

- [ ] **SQL 실력 확인**
  - window function, group by, FILTER, CTE 복습

- [ ] **데이터 파이프라인 설계 개념**
  - validation, retry, idempotency, backfill, monitoring

- [ ] **수요예측 도메인 기초 6개 개념 숙지**
  - 특히 stockout bias 설명 가능하게

- [ ] **역질문 3개 이상 준비**

---

### 면접에서 전달할 핵심 메시지

> **"수요예측 서비스의 가치는 예측값 자체보다, 그 예측값이 고객의 실제 의사결정과 업무 자동화로 연결될 때 발생한다고 생각합니다."**

> **"저는 데이터를 수집하고 계산한 뒤 API와 화면으로 제공하는 end-to-end 흐름을 직접 구현해봤고, AI 팀의 결과를 안정적으로 서비스화하는 역할에 기여하고 싶습니다."**

---

*문서 작성 기준: mofl 채용공고(잡코리아/원티드/링크드인), mofl 공식 사이트, 서울AI허브, 한경매거진 인터뷰 교차 분석*
